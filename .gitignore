.devenv/
.direnv/
.envrc.local
devenv.local.nix

# Local secrets / configuration (never commit).
.env
.env.*
!.env.example
config.json
config.local.json
config.*.local.json

# Mounted data directory: human-edited whitelist and worker review artifacts.
# Contains derived personal metadata; never commit.
data/

# Build output
dist/

# Dependencies
node_modules/

# OS / editor noise
.DS_Store
